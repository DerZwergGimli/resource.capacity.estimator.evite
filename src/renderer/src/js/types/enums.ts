export enum host_resource_types {
  cpu = 'cpu',
  ram = 'ram',
  storage = 'storage'
}

export enum vm_resource_types {
  vcpu = 'vcpu',
  vram = 'vram',
  vstorage = 'vstorage'
}

export enum system_dimensioning_types {
  min = 'min',
  rec = 'rec',
  max = 'max'
}
